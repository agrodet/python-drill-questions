import random
import math

numbers = []
for i in range(0, 50):
    numbers.append(random.randint(0, 100))

# 重複を削除する
numbers = """①"""(numbers)

# 平方数を見つける
squares = [n for n in numbers if math.sqrt(n).is_"""②"""()]

# リストの要素を確認するコード
print(numbers)
print(squares)
